sudo: false
language: ruby
cache: bundler
rvm:
- 2.4.2

install:
  # Install Jekyll and dependencies
  - bundle install

script: bundle exec rake site:deploy --quiet #--trace

# Generate your secure token with the travis gem:
# get Github token from your Travis CI profile page
gem install travis
# GH_TOKEN from https://github.com/settings/tokens
travis encrypt 'GIT_NAME="bmanohar16" GIT_EMAIL="manoharan.balakumaran@gmail.com" GH_TOKEN=d4a1be87507c3abb1a0aa15d2a8c1f7fe134eb85'
env:
  global:
    secure: 
branches:
  only:
    - master

